import React from 'react';
import { Box, Typography } from '@mui/material';

const OrgDashboard = () => (
  <Box p={3}>
    <Typography variant="h4">Welcome to the Organization Dashboard</Typography>
    <Typography variant="body1" mt={2}>Here you can manage your job postings and view applicants.</Typography>
  </Box>
);

export default OrgDashboard;
